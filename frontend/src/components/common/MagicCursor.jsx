import React from 'react';

const MagicCursor = () => {
  // Returning null completely disables the custom cursor.
  // Your browser will naturally fall back to the default system arrow!
  return null;
};

export default MagicCursor;